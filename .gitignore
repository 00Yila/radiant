node_modules/
dist/
.astro/

# Generated by scripts/export-catalogue-for-php.mjs on every `npm run build`
# — never hand-edited, and always regenerated fresh before a deploy.
public/data/catalogue/products.json
.env
.env.*
.DS_Store
test-results/
playwright-report/

# Real Paystack/DB/CSRF secrets — see public/config/radiant-alpha.php.example
# for the shape. Never commit the filled-in file, in either location
# public/_lib/config.php can resolve it from.
public/config/radiant-alpha.php
config/radiant-alpha.php

# Admin Basic Auth — set up via hPanel's Password Protect Directories after
# deploy (docs/admin-setup.md), never hand-authored or committed here.
public/admin/.htaccess
